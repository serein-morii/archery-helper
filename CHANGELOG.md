# 更新日志

## 1.0.7（2026-09-20）

- 布局与间距整体打磨：统一 4px 间距节奏（视图内边距、查询栏/标签栏/工具栏横向内边距对齐 12px、登录横幅与侧边栏留白）、页面标题层级收敛（17→16px）、查询栏「Limit」标签中文化为「行数上限」、统计条与表单间距微调

## 1.0.6（2026-09-20）

- 收藏云端 ⇄ 本地双向同步：本地 SQL 可上推为 Archery 云端收藏（名称存别名、分组写入 SQL 头注释，仅同步只读语句）；云端收藏可一键导入本地（自动去重，归入「云端收藏」分组）；已同步条目带「已同步」标记

## 1.0.5（2026-09-20）

- 搜索联想全面升级（编辑器补全 / 可搜索下拉 / Ctrl+K 命令面板）：改为包含匹配并按「命中位置优先、更短的排前面」排序（输入 staff 时 staff 排第一，astaff 靠后），命中片段高亮显示；下拉搜索时平铺不显示分组标题
- 新增「本地 SQL 收藏」：查询后在结果工具栏点「存本地」命名保存（仅存本机 chrome.storage，不影响服务端），支持自定义分组、分组管理、按分组筛选；收藏页改为「云端收藏 / 本地 SQL」双页签，本地条目可一键回填实例/库并执行，支持改名、改分组、改 SQL、删除；支持导出 / 导入 JSON（按名称+SQL+分组去重合并，方便换机迁移）
- EXPLAIN 执行计划结果智能着色：全表扫描（type=ALL）红、走索引绿、大扫描量（rows≥1万/10万）橙黄、filesort/temporary 红标；修复着色在普通单元格上的空 class 报错；执行计划按钮移到「运行查询」旁边
- 图表修复与增强：字符串数值（decimal / 聚合结果等）正确解析不再一律按 0 处理，NULL 不画柱/折线断开而非落到 0，默认 X 轴自动选分类列、Y 轴识别数值列更准（支持千分位数字）
- 运行查询按钮去掉「Ctrl ↵」角标（悬停提示即可），与执行计划按钮同尺寸
- 导出数据字典完成后自动弹窗预览（Markdown 渲染）并附下载按钮，不再「无声无息」
- 编辑器「收藏」按钮升级：点击先选「存到本地（分组+命名）」或「存到云端（直接命名）」，弹窗含 SQL 预览；结果栏「存本地」按钮保持不变
- 表结构对比弹窗重构：建表语句改为单一行对齐视图（公共行对齐、仅 A 红 / 仅 B 蓝、同字段定义不同黄），不再是两个独立块；对比弹窗加宽（最大 1160px）
- 数据浏览器收起/展开重做：左侧导航栏常驻「收起/展开」按钮（皮肤切换上方），收起后高亮提示；侧边栏标题栏的旧收起按钮移除
- 实例等下拉框收窄（最宽 340px，不再撑满整栏）；单表对比底部按钮文字改为「查看完整建表语句」
- 对象树美化：实例绿 / 库琥珀 / 表蓝图标着色、虚线层级参考线、当前表高亮左侧指示条、子级展开动画、计数徽章描边
- 按钮/图标按钮增加按压缩放反馈，悬停阴影更立体

## 1.0.4（2026-09-20）

- 扩展图标更换为新设计（绿底徽标：靶心箭矢 + 轨道 + 机器人），圆外白底透明化，深浅色工具栏均无白框

## 1.0.3（2026-09-20）

- 查看表结构不再自动放大结果区（编辑器保持可见，需要放大时手动点结果区放大按钮）

## 1.0.2（2026-09-20）

- 头像换新人形图标（清新绿渐变底、可点击反馈），工具栏 PNG 图标配色同步更新
- popup 已连接时显示当前登录用户名（纯浏览器会话模式自动从查询记录取显示名）

## 1.0.1（2026-09-20）

**查询与编辑**
- SQL 格式化器重写：中文别名不再被拆开、`a.b` 点号紧贴、LEFT JOIN / GROUP BY 等多词子句成对识别、CASE WHEN 标准缩进、IN 列表与分组括号保持行内、函数括号紧贴、末尾分号去重
- 多查询标签页（草稿独立保存、恢复）
- EXPLAIN 执行计划按钮（按实例类型自动拼接）
- 大数据量分批流式查询：5000 行/批、实时进度、可停止、10 万行安全上限
- 智能补全：联想框跟随光标、支持反引号表名、字段兜底 information_schema
- 命令面板 Ctrl+K：功能 / 实例 / 库 / 表 / 最近 SQL 全局搜索跳转

**结果增强**
- 右键列：生成 IN 列表 / 复制整列 / 结果转批量 INSERT 语句（自动解析目标表）
- 右键行：生成 INSERT / UPDATE 语句（主键定位，只生成不执行）
- 单元格 JSON 格式化、双击复制
- 结果图表可视化（柱状 / 折线）、两个结果集对比

**数据理解**
- 表结构三视图：字段 / 建表语句 / 索引，查看时自动放大结果区
- 字段搜索：按字段名搜当前库全表（含类型与注释）
- 右键库导出全库数据字典（Markdown）
- 表结构对比：单表字段级差异（仅A/仅B/定义不同/一致）+ 整库清单对比（快，不扫结构）+ 深度对比（按需）；弹窗支持建表语句 / 字段表格左右切换

**工单与发版**
- SQL 审核检测 → 一键提交上线工单（资源组自动解析）
- 工单审批：通过 / 驳回 / 立即执行 / 终止
- 工单列表分页修复（改 POST，服务端分页生效）

**登录与安全**
- 2FA 完整支持：动态码登录 + TOTP 密钥全自动（设置/弹窗/登录横幅三处可填）
- 会话免打扰：有浏览器会话直接用；无会话才弹登录

**界面**
- 全部下拉框可搜索（combo 组件）
- 查询历史 SQL 收缩态（单行截断、点击展开、行等高）
- 清新绿主题、图标升级
- 诊断视图：表空间 TOP + InnoDB 事务（长事务预警）
- 设置内可查看更新日志

## 1.0.0（2026-09-20）

- 初始版本：SQL 工作台、数据浏览器、查询历史、收藏、审核检测、上线工单、导出（CSV/Excel/JSON）、深浅主题
