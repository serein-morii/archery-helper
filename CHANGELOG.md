# 更新日志

## 1.1.2（2026-09-21）

- 顶部搜索支持搜全库的表与字段：本地缓存对象索引（chrome.storage，新增 unlimitedStorage 权限）；数据浏览器新增「重建搜索索引」按钮（全实例库表清单，带进度）；查询某库时自动增量更新该库（含字段，information_schema 一次拉全库，7 天过期自动重建）；库右键可单独重建该库索引；搜索命中显示 实例/库 并一键跳转表结构
- Excel 导出升级为双 Sheet：第一个 Sheet 为查询结果，第二个「导出信息」Sheet 含 SQL、实例/库、导出行数、总行数、列数、查询耗时、导出时间与版本（同时修复旧 HTML 格式长数字被 Excel 转科学计数的问题，数据一律按文本保真）

## 1.1.1（2026-09-21）

- 单表字段级对比的「查看完整建表语句」按钮改为吸底固定，字段多时不用滚动到底即可点击

## 1.1.0（2026-09-21）

- 收藏合并为统一列表：去掉「云端收藏 / 本地 SQL」双页签与同步弹窗，每条收藏带「云端」开关——勾选即保存到 Archery 收藏（跨设备可见），取消勾选则从云端移除、本地保留；编辑（改名/换分组/改 SQL）后自动重推云端并取消旧收藏；卡片带「云端 / 待同步」徽章与「存到云端 / 取消云端」操作；打开收藏页自动从云端拉取其它设备新增与网页端手动收藏（按标记 gid 去重，本地内容为准），工具栏保留手动刷新、搜索（名称/SQL/分组/实例）、分组管理、导入导出 JSON
- 修复收藏页列表无法滚动：合并页签时滚动规则仍绑定在已删除的旧容器选择器上，超出视口的卡片被直接裁切；现滚动固定在卡片列表内，工具栏与分组筛选不被卷走
- 首次安装后第一次打开工作台自动弹出更新日志；已有用户重新加载扩展不会再弹
- 检测到 GitHub 有新版本时第一次自动弹出更新提醒；关掉后该版本不再自动弹（左上角「新」与设置里「检测版本」仍可手动打开）
- README 收藏说明改为统一列表（去掉过时的双页签/双向同步描述）

## 1.0.9（2026-09-20）

- 列表页空状态与滚动：历史 / 云端收藏 / 工单加载失败或无数据时改为居中空提示，不再留下一整块空白表；收藏页本地卡片列表可滚动，不再被底部裁切
- 表结构对比改为「环境 A / 环境 B」双栏，避免六个下拉挤在一行；未选择时给出空提示
- 编辑器光标行列改到工具栏右侧，不再占用结果区耗时位置；侧栏主题/设置补上文字标签；页脚去掉重复 id
- 对象树当前实例/库/表会高亮并展开祖先节点；快捷键说明补上 Ctrl+K
- 弹窗账号副文案改为最多两行、提示不再把长地址硬生生拆开
- 云端收藏改为与本地 SQL 同一套卡片列表（标题/分组标签、SQL 两行预览、查询/回填/取消收藏），不再用表格
- 查询结果图表的「返回表格」改到结果工具栏，与「存本地 / 图表 / 对比」同一行对齐，不再挤在坐标轴下拉里
- 左上角版本号检测 GitHub 仓库：有新版本时徽章显示「新」，点击弹出更新说明并可下载更新包 / 重新加载扩展；无更新则与现在一样只显示当前版本（失败静默，6 小时缓存一次）；设置里可手动点「检测版本」立刻对照仓库
- 修复本地 SQL 上推云端时 LIMIT 被改成 1：此前为少跑数据把 `limit_num` 固定成 1，Archery 会把语句末尾的 `LIMIT 1000` 重写成 `LIMIT 1` 再写入查询日志；现改为沿用原 SQL 的 LIMIT（没有则不限）
- 云端同步标记改为中性书签注释并在执行前剥离：上推时标记以 `/* bookmark id=… group=… name=… */`（明文、不含插件名，审计看起来只是普通书签注释）注入第一条语句关键字之后；执行查询 / EXPLAIN / 审核检测前自动移除标记，云端收藏回填执行的 SQL 不带插件痕迹；重复标记自动去重；三种历史格式（首行块注释、首行 `-- ` 行注释、语句内 archery-helper 注释）云端识别与本地剥离均兼容
- 修复格式化器破坏多字符运算符：tokenizer 此前逐字符切分符号，`>=` 被拼成 `> =`、`<>` 拼成 `< >`，产生 MySQL 1064 语法错误；现把 `>= <= <> != := || << >> -> ->> =>` 等识别为整体 token，`INTERVAL (` 不再被误当函数贴合；实测原 SQL 格式化后可正常执行，二次格式化结果稳定

## 1.0.8（2026-09-20）

- 首次使用自动识别地址：未配置地址时打开弹窗，自动探测当前标签页是否为 Archery 站点（抓取 `/login/` 按标题与品牌链接特征判断），命中即自动采用并直接完成连接检测；非 Archery 页面则保留手动填写引导，并提示「先打开 Archery 页面再点开弹窗」
- 「保存凭证并登录」按钮美化：升级为品牌渐变主按钮（撑满、居中白字、光晕），与「打开 SQL 工作台」风格统一；登录过程中自动变为「正在连接…」并显示旋转加载圈，替代原先的半透明禁用态
- 修复「编辑本地 SQL」弹窗 SQL 框在暗黑模式下显示为白色：textarea 此前未纳入主题化输入框样式，补齐深/浅色边框、背景与聚焦光圈；同时为两主题声明 `color-scheme`，原生控件（下拉、滚动条）随主题渲染
- 云端同步标记改为 SQL 语句内块注释：上推时目录标记 `/* archery-helper gid=… group=… name=… */` 注入到第一条语句关键字（SELECT/SHOW/…）之后；语句前的首行注释（块注释或 `-- ` 行注释）会被部分实例的查询校验拒绝（disable_star 开启时报「SQL语句中含有 *」），语句内注释可正常执行且查询日志保留原文，云端→本地仍可识别；旧的首行块注释/行注释格式均兼容，此前上推失败的条目下次同步自动用新格式重试
- 收藏页整体重构：页头新增「保存编辑器 SQL / 同步」快捷操作；本地 SQL 由表格改为卡片列表（SQL 预览两行截断点击展开、行内 查询/编辑/删除、空状态引导）；分组筛选改为带计数的 chips（全部/未分组/各分组，选中高亮）；导出/导入改为图标按钮；同步弹窗两个方向按钮升级为品牌色徽章卡片式
- 修复刷新后已选实例/库丢失：实例切换存在「change 事件隐式链 + 显式调用」双链竞态，后到的库列表响应会重建下拉并清掉已选的库，且恢复流程中途的防抖保存会把空库写进草稿；现改为单链加载（selectInstance）+ 过期响应丢弃（序号守卫）+ 恢复完成后立即回写草稿，并补上 PgSQL schema 的恢复
- 修复左侧树选择不带实例：树实例节点此前静默设 select.value，combo 显示层不刷新导致界面仍显示旧实例；树的实例/库节点、历史与收藏回填、本地 SQL 回填、Ctrl+K 跳转统一走 selectInstance，选项值与显示标签同步更新，去掉树选库时 250ms 定时等待的临时手段

## 1.0.7（2026-09-20）

- 布局与间距整体打磨：统一 4px 间距节奏（视图内边距、查询栏/标签栏/工具栏横向内边距对齐 12px、登录横幅与侧边栏留白）、页面标题层级收敛（17→16px）、查询栏「Limit」标签中文化为「行数上限」、统计条与表单间距微调

## 1.0.6（2026-09-20）

- 收藏云端 ⇄ 本地双向同步：本地 SQL 可上推为 Archery 云端收藏（名称存别名、分组写入 SQL 头注释，仅同步只读语句）；云端收藏可一键导入本地（自动去重，归入「云端收藏」分组）；已同步条目带「已同步」标记

## 1.0.5（2026-09-20）

- 搜索联想全面升级（编辑器补全 / 可搜索下拉 / Ctrl+K 命令面板）：改为包含匹配并按「命中位置优先、更短的排前面」排序（输入 staff 时 staff 排第一，astaff 靠后），命中片段高亮显示；下拉搜索时平铺不显示分组标题
- 新增「本地 SQL 收藏」：查询后在结果工具栏点「存本地」命名保存（仅存本机 chrome.storage，不影响服务端），支持自定义分组、分组管理、按分组筛选；收藏页改为「云端收藏 / 本地 SQL」双页签，本地条目可一键回填实例/库并执行，支持改名、改分组、改 SQL、删除；支持导出 / 导入 JSON（按名称+SQL+分组去重合并，方便换机迁移）
- EXPLAIN 执行计划结果智能着色：全表扫描（type=ALL）红、走索引绿、大扫描量（rows≥1万/10万）橙黄、filesort/temporary 红标；修复着色在普通单元格上的空 class 报错；执行计划按钮移到「运行查询」旁边
- 图表修复与增强：字符串数值（decimal / 聚合结果等）正确解析不再一律按 0 处理，NULL 不画柱/折线断开而非落到 0，默认 X 轴自动选分类列、Y 轴识别数值列更准（支持千分位数字）
- 运行查询按钮去掉「Ctrl ↵」角标（悬停提示即可），与执行计划按钮同尺寸
- 导出数据字典完成后自动弹窗预览（Markdown 渲染）并附下载按钮，不再「无声无息」
- 编辑器「收藏」按钮升级：点击先选「存到本地（分组+命名）」或「存到云端（直接命名）」，弹窗含 SQL 预览；结果栏「存本地」按钮保持不变
- 表结构对比弹窗重构：建表语句改为单一行对齐视图（公共行对齐、仅 A 红 / 仅 B 蓝、同字段定义不同黄），不再是两个独立块；对比弹窗加宽（最大 1160px）
- 数据浏览器收起/展开重做：左侧导航栏常驻「收起/展开」按钮（皮肤切换上方），收起后高亮提示；侧边栏标题栏的旧收起按钮移除
- 实例等下拉框收窄（最宽 340px，不再撑满整栏）；单表对比底部按钮文字改为「查看完整建表语句」
- 对象树美化：实例绿 / 库琥珀 / 表蓝图标着色、虚线层级参考线、当前表高亮左侧指示条、子级展开动画、计数徽章描边
- 按钮/图标按钮增加按压缩放反馈，悬停阴影更立体

## 1.0.4（2026-09-20）

- 扩展图标更换为新设计（绿底徽标：靶心箭矢 + 轨道 + 机器人），圆外白底透明化，深浅色工具栏均无白框

## 1.0.3（2026-09-20）

- 查看表结构不再自动放大结果区（编辑器保持可见，需要放大时手动点结果区放大按钮）

## 1.0.2（2026-09-20）

- 头像换新人形图标（清新绿渐变底、可点击反馈），工具栏 PNG 图标配色同步更新
- popup 已连接时显示当前登录用户名（纯浏览器会话模式自动从查询记录取显示名）

## 1.0.1（2026-09-20）

**查询与编辑**
- SQL 格式化器重写：中文别名不再被拆开、`a.b` 点号紧贴、LEFT JOIN / GROUP BY 等多词子句成对识别、CASE WHEN 标准缩进、IN 列表与分组括号保持行内、函数括号紧贴、末尾分号去重
- 多查询标签页（草稿独立保存、恢复）
- EXPLAIN 执行计划按钮（按实例类型自动拼接）
- 大数据量分批流式查询：5000 行/批、实时进度、可停止、10 万行安全上限
- 智能补全：联想框跟随光标、支持反引号表名、字段兜底 information_schema
- 命令面板 Ctrl+K：功能 / 实例 / 库 / 表 / 最近 SQL 全局搜索跳转

**结果增强**
- 右键列：生成 IN 列表 / 复制整列 / 结果转批量 INSERT 语句（自动解析目标表）
- 右键行：生成 INSERT / UPDATE 语句（主键定位，只生成不执行）
- 单元格 JSON 格式化、双击复制
- 结果图表可视化（柱状 / 折线）、两个结果集对比

**数据理解**
- 表结构三视图：字段 / 建表语句 / 索引，查看时自动放大结果区
- 字段搜索：按字段名搜当前库全表（含类型与注释）
- 右键库导出全库数据字典（Markdown）
- 表结构对比：单表字段级差异（仅A/仅B/定义不同/一致）+ 整库清单对比（快，不扫结构）+ 深度对比（按需）；弹窗支持建表语句 / 字段表格左右切换

**工单与发版**
- SQL 审核检测 → 一键提交上线工单（资源组自动解析）
- 工单审批：通过 / 驳回 / 立即执行 / 终止
- 工单列表分页修复（改 POST，服务端分页生效）

**登录与安全**
- 2FA 完整支持：动态码登录 + TOTP 密钥全自动（设置/弹窗/登录横幅三处可填）
- 会话免打扰：有浏览器会话直接用；无会话才弹登录

**界面**
- 全部下拉框可搜索（combo 组件）
- 查询历史 SQL 收缩态（单行截断、点击展开、行等高）
- 清新绿主题、图标升级
- 诊断视图：表空间 TOP + InnoDB 事务（长事务预警）
- 设置内可查看更新日志

## 1.0.0（2026-09-20）

- 初始版本：SQL 工作台、数据浏览器、查询历史、收藏、审核检测、上线工单、导出（CSV/Excel/JSON）、深浅主题
